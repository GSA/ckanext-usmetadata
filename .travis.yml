#soon ...
#sudo: false
language: python
python:
    - "2.7"
env:
    - CKANVERSION=inventory
install:
    - bash bin/travis-build.bash
    - pip install coveralls
script: sh bin/travis-run.sh
after_success:
    - coveralls